version: 2

jobs:
  build:
    docker:
      - image: circleci/node:10.4-browsers
    steps:
      - checkout
      - run: yarn install
      - run: yarn run build
      - run: yarn run lint
      - run: yarn run prettier-check
